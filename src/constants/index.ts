/**
 * constants/index.ts
 * 
 * Arquivo de exportação centralizado para todas as constantes da aplicação
 * Re-exporta todos os textos, cores e configurações dos arquivos específicos
 */

// Re-exporta todos os textos
export { TEXTS } from './texts';

// Re-exporta todas as cores
export { COLORS, CHART_COLORS } from './colors';

// Re-exporta todas as configurações
export { CONFIG, ROUTES, BREAKPOINTS, Z_INDEX } from './config'; 